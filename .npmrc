# Ensure workspace packages are linked even when using semver ranges.
# This keeps local development working before the packages are published.
link-workspace-packages=true
prefer-workspace-packages=true
